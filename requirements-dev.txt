# CI Python tooling, compiled to requirements-dev.lock with uv
# (--generate-hashes) so workflows can install with --require-hashes
# (OpenSSF Scorecard Pinned-Dependencies). Versions marked ADR-0002 are
# the deliberate inline pins this file replaces; the rest resolve to the
# latest compatible release at lock-generation time.
ruff==0.15.22                       # ADR-0002
mypy==2.3.0                        # ADR-0002
types-PyYAML==6.0.12.20260518      # ADR-0002
cyclonedx-bom==7.3.0               # ADR-0002 (SBOM, ADR-0004)
radon
pytest
pytest-cov
pip-audit
