---
author: "contact@sebastienrousseau.com (Sebastien Rousseau)"
banner_alt: "エンタープライズグレードの静的サイトジェネレーターのアーキテクチャロードマップを表す、抽象的な技術的背景。"
banner_height: "1280"
banner_width: "1920"
banner: "https://cloudcdn.pro/stocks/images/gemini-background.webp"
cdn: "https://cloudcdn.pro"
charset: "UTF-8"
cname: "sebastienrousseau.com"
copyright: "© Copyright 2025 - 2026 - Sebastien Rousseau. All rights reserved."
date: "July 22, 2026"
description: "Rust製静的サイトジェネレーターの徹底分析。コンパイル時セキュリティ、WCAGゲート、ローカルファーストAI、v0.0.41のギャップ、そして1.0へのエンタープライズロードマップ。"
format-detection: "telephone=no"
hreflang: "ja"
icon: "https://cloudcdn.pro/clients/sebastienrousseau/v1/logos/sebastienrousseau.svg"
id: "https://sebastienrousseau.com/ja/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
image_alt: "セバスチャン・ルソーのモノクロ肖像写真"
image_height: "162"
image_width: "162"
image: "https://cloudcdn.pro/stocks/images/sebastienrousseau.webp"
keywords: "静的サイトジェネレーター, Rust, forbid unsafe_code, WCAG 2.2 AA, Content Security Policy, Subresource Integrity, SLSA, CycloneDX SBOM, ローカルLLMパイプライン, DORA, インクリメンタルビルド, lol_html, WASMプラグインサンドボックス, セマンティックベクトル検索, MiniJinja, Ollama"
language: "ja"
last_reviewed: "2026-07-22"
layout: "report"
locale: "ja_JP"
logo_alt: "セバスチャン・ルソーのロゴ"
logo_height: "44"
logo_width: "44"
logo: "https://cloudcdn.pro/clients/sebastienrousseau/v1/logos/sebastienrousseau.svg"
menu: ""
measurementID: "G-169G4ET5HQ"
name: "Sebastien Rousseau"
permalink: "https://sebastienrousseau.com/ja/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
rating: "general"
referrer: "no-referrer"
robots: "index, follow"
schema: "FAQPage, Article"
seo_title: "静的サイトジェネレーター：1.0への道のり"
short_name: "sebastienrousseau"
subtitle: "セキュア・バイ・デフォルトのインフラとして構築されたRust製静的サイトジェネレーターのアーキテクチャ監査とロードマップ。v0.0.41が実際に提供する機能とREADMEの約束との差、不足する5つのエンタープライズ機能、そしてDORAおよびEAAに準拠した1.0への段階的な道筋。"
tags: "静的サイトジェネレーター, Rust, Webセキュリティ, アクセシビリティ, DORA, サプライチェーン, SLSA, ローカルAI, WCAG, コンパイル時, ロードマップ, エンタープライズ"
theme-color: "0, 83, 191"
title: "静的サイトジェネレーター（SSG）：エンタープライズグレードの戦略的徹底分析とアーキテクチャロードマップ"
url: "https://sebastienrousseau.com/ja/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
viewport: "width=device-width, initial-scale=1, shrink-to-fit=no"
atom_link: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap/rss.xml"
category: "Technology"
docs: "https://validator.w3.org/feed/docs/rss2.html"
generator: "Static Site Generator (SSG) (version 0.0.26)"
item_description: "Rust製静的サイトジェネレーターの徹底分析。コンパイル時セキュリティ、WCAGゲート、ローカルファーストAI、v0.0.41のギャップ、そして1.0へのエンタープライズロードマップ。"
item_guid: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap/rss.xml"
item_link: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap/rss.xml"
item_pub_date: "Wed, 22 Jul 2026 07:07:07 +0000"
item_title: "静的サイトジェネレーター（SSG）：エンタープライズグレードの戦略的徹底分析とアーキテクチャロードマップ"
last_build_date: "Wed, 22 Jul 2026 06:06:06 +0000"
managing_editor: "contact@sebastienrousseau.com (Sebastien Rousseau)"
pub_date: "Wed, 22 Jul 2026 07:07:07 +0000"
ttl: "60"
type: "article"
webmaster: "contact@sebastienrousseau.com"
apple_mobile_web_app_orientations: "portrait"
apple_touch_icon_sizes: "192x192"
apple-mobile-web-app-capable: "yes"
apple-mobile-web-app-status-bar-inset: "black"
apple-mobile-web-app-status-bar-style: "black-translucent"
apple-mobile-web-app-title: "SSG 1.0への道"
apple-touch-fullscreen: "yes"
msapplication-navbutton-color: "0, 83, 191"
twitter_card: "summary_large_image"
twitter_creator: "@wwdseb"
twitter_description: "コンパイル時WCAGゲート、SHA-384 SRI、CSPインジェクション、ローカルファーストLLMパイプラインがこのRustエンジンを際立たせる。v0.0.41の率直なギャップ分析とエンタープライズグレード1.0への道。"
twitter_image: "https://cloudcdn.pro/clients/sebastienrousseau/v1/logos/sebastienrousseau.svg"
twitter_image_alt: "セバスチャン・ルソーのロゴ"
twitter_site: "@wwdseb"
twitter_title: "静的サイトジェネレーター：1.0への道のり"
twitter_url: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
excerpt: "コンパイル時WCAGゲート、SHA-384 SRI、CSPインジェクション、ローカルファーストLLMパイプラインがこのRustエンジンを際立たせる。しかしインクリメンタルビルド、ネイティブ圧縮、AVIFはまだ構想段階だ。率直なギャップ分析と1.0への道を示す。"
author_website: "https://sebastienrousseau.com"
author_twitter: "@wwdseb"
author_location: "London, UK"
thanks: "お読みいただきありがとうございます！"
site_last_updated: "2026-07-22"
site_standards: "HTML5, CSS3, RSS, Atom, JSON, XML, YAML, Markdown, TOML"
site_components: "Kaishi, Kaishi Builder, Kaishi CLI, Kaishi Templates, Kaishi Themes"
---

# 静的サイトジェネレーター（SSG）：エンタープライズグレードの戦略的徹底分析とアーキテクチャロードマップ

*調査日：2026-06-22。v0.0.41時点の `static-site-generator` のコードベース検査、および2026年のSSG動向に関するWeb調査に基づく。*

**規制対象の発行者にとって、静的サイトジェネレーターはもはやデザインツールではありません。それは運用リスクの境界線（オペレーショナルリスク・ペリメーター）の一部です。** オープンソースのRust製 [static-site-generator](https://github.com/sebastienrousseau/static-site-generator) はこの前提の上に構築されており、セキュリティ、アクセシビリティ、国際化、AIコンテンツパイプラインをコンパイル時へと移すことで、チェックに失敗すればビルドが停止し、本番環境に到達しないようにしています。本稿では、バージョン0.0.41が実際に提供している機能と、ドキュメントがいまだ約束にとどめている機能とを切り分け、同ジェネレーターがまだ備えていない5つのエンタープライズ機能を提示し、DORA、欧州アクセシビリティ法、そして現代のサプライチェーン標準に準拠した1.0リリースへの段階的な道筋を提案します。

<!-- lead-start -->
<aside class="post-lead" aria-label="記事の要約">
<p class="post-lead-tldr"><strong>要点。</strong> Rust製 <code>static-site-generator</code> は、Web公開を監査可能でセキュア・バイ・デフォルトのソフトウェアパイプラインとして扱います。すなわち、ワークスペース全体での <code>forbid(unsafe_code)</code>、SHA-384のSubresource Integrity、Content Security Policyの抽出、コンパイル時のWCAG 2.2 AAゲート、そしてローカルファーストのLLMパイプラインです。v0.0.41のコード検査からは、ドキュメント化された複数の機能がいまだ構想段階にあることが分かります。ネイティブ圧縮、インクリメンタル再ビルド、AVIFがその例です。本稿はその率直なギャップ分析であり、エンタープライズグレードの1.0に向けた段階的ロードマップです。</p>
<p class="post-lead-heading"><strong>主な要点</strong></p>
<ul class="post-lead-takeaways">
  <li><strong>セキュリティとアクセシビリティのモデルは実在します。</strong> コンパイル時のSRI、CSP抽出、Sigstore証跡とCycloneDX SBOMを伴う署名付きリリース、さらにビルドを停止させるWCAG 2.2 AAゲートは、ドキュメントに書かれているだけでなくコードとして実装されています。</li>
  <li><strong>いくつかの目玉機能はそうではありません。</strong> 圧縮機能は空白を詰めるだけのものであり、インクリメンタルビルドを駆動するはずの依存関係グラフは本番では一度も投入されず、AVIFエンコーディングは空のベクターを返すスタブです。</li>
  <li><strong>5つのエンタープライズ機能が欠けています。</strong> WASMプラグインのサンドボックス化、ストリーミング型ゼロコピーHTMLリライター、ローカルセマンティック検索、決定論的な推論キャッシュ、そして非同期ファイルI/Oです。</li>
  <li><strong>ロードマップはリスクに沿って順序付けられています。</strong> 正確性のパッチ（0.0.42）、信頼性とインクリメンタルのマイナー版（0.1.0）、続いてサンドボックス、セマンティック検索、SLSA v1.1のプロベナンスを担うエンタープライズ向けメジャー版（1.0.0）です。</li>
</ul>
<p class="post-lead-related"><strong>関連する読み物：</strong> <a href="https://sebastienrousseau.com/2026-07-03-emerging-technology-risk-horizon-banks-2026">銀行にとっての新興技術リスクの地平</a>、<a href="https://sebastienrousseau.com/2026-07-07-corporate-banking-api-standard-agentic-mcp-2026">エージェント型MCPのための法人銀行API標準</a>。</p>
</aside>
<!-- lead-end -->

> **エグゼクティブサマリー**
>
> - **公開はいまや運用リスクの境界線です。** DORA、欧州アクセシビリティ法、GDPRのもとでは、外部に公開されるあらゆる資産が、サプライチェーン侵害、改ざん、規制上の露出への潜在的な侵入口となります。コンパイル時モデルは、準拠していない出力を出荷前に拒否することで、この境界線を狭めます。
> - **本エンジンの差別化要因は、ドキュメント上の願望ではなく、コンパイラによって強制されます。** ワークスペース全体での `forbid(unsafe_code)`、本物のSHA-256/384 SRI、自動的なCSP抽出、そしてビルド時のWCAG 2.2 AAゲートは、セキュリティとアクセシビリティを事後監査から、ビルドを確実に失敗させる仕組みへと変えます。
> - **バージョン0.0.41にはドキュメントとコードの間に乖離があります。** ネイティブ圧縮、依存関係グラフによるインクリメンタル再ビルド、AVIFサポートは記載されているものの機能していません。本稿は各ギャップを正確なソース位置とともに指摘します。
> - **1.0への道は、願望リストではなく順序立てられた工程です。** まず堅牢性（0.0.42）、次にインクリメンタルの正確性（0.1.0）、そして規制対象の購入者が求めるエンタープライズ機能、すなわちWASMサンドボックス化、ローカルセマンティック検索、検証可能なSLSAプロベナンス（1.0.0）です。

## 現在の強み

`static-site-generator` のコードベースは、旧来のJavaScriptおよびGo製エンジンと一線を画す、いくつかの特徴的なエンジニアリング上の判断を示しています。

- **コンパイル時のセキュリティ姿勢：** ワークスペース全体での `#![forbid(unsafe_code)]` が、コンパイル時のメモリ安全性を保証します。ビルドパイプラインは本物のSHA-256/SHA-384のSubresource Integrity（SRI）ハッシュ（`src/plugins/assets.rs`）を生成し、unsafe-inlineのスクリプトとスタイルを除去する自動的なContent Security Policy（CSP）抽出を行います。リリースは署名され、Sigstoreの証跡を伴い、ビルドごとにCycloneDX 1.5のSBOMを生成します。  
- **コンパイラによって強制されるアクセシビリティゲート：** Webコンテンツアクセシビリティ・ガイドライン（WCAG）2.2レベルAAのチェックは、Playwrightによって駆動されるビルド時のaxe-coreパーサーを介してコンパイルパイプライン内で実行されます。アクセシビリティは公開後の監査ではなく、確実なビルドゲートとなります。ページが失敗すると、正確な行番号のエラーとともにコンパイルが停止します。  
- **データ主権を備えたAIパイプライン：** ローカルLLMによる翻訳とメタデータ抽出のパイプライン（ローカルのOllamaまたはllama.cppエンドポイント経由）により、機関は決算前の開示情報や機密性の高い知的財産をパブリッククラウドのAI APIに送信することなく、コンテンツ要約、JSON-LDスキーマ生成、多言語翻訳を自動化できます。  
- **並列化されたコンパイル：** Rustのメモリ安全性の保証が、Rayonによって駆動される並列化されたHTMLおよびアセットのパイプライン（`src/core/pipeline.rs`）を支えています。プラグインパイプラインは融合された変換を実行し、`SearchPlugin`、`SeoPlugin`、`CanonicalPlugin`、`JsonLdPlugin` が `par_iter()` の上で動作するため、各ページはディスクへの読み書きが一度で済みます。  
- **サプライチェーンと依存関係の衛生：** テンプレートエンジンをTeraからMiniJinjaへ移行（`v0.0.37`）したことで、バイナリサイズが削減され、`rand` などの推移的依存関係がコンパイル時に取り除かれ、ソフトウェアサプライチェーンの露出を下げるコンパクトな依存関係フットプリントが実現しました。

---

## ギャップと現実

これらの卓越した強みがある一方で、v0.0.41の厳密なコードベース検査からは、ドキュメントの主張と実際のRustコードとの間に、アーキテクチャ上、機能上、そして開発者体験上のいくつかのギャップが明らかになります。

### アーキテクチャ上のギャップ

- **空白の圧縮 対 ネイティブ圧縮：** READMEは「ネイティブなJS/CSS圧縮」を約束していますが、`MinifyPlugin`（`src/plugins/plugins.rs:96-116`）は単純に空白を詰めるだけのものにすぎません。`<pre>` 要素では処理を打ち切り、HTML内の連続した空白を詰めますが、構文を認識したネイティブなCSSやJSの圧縮は行いません。さらに、トップレベルのページのみを処理し、サブディレクトリ（`/blog/` や `/tags/` など）を再帰的にたどらないため、深い階層のページは圧縮されないまま残ります。  
- **死んだインクリメンタル基盤：** 依存関係追跡グラフ（`src/core/depgraph.rs` 内の `DepGraph`）はコンパイルされ `PluginContext.dep_graph` に読み込まれますが、本番コードでは一度も実際に投入されません。メソッド `add_dep()` はユニットテストでしか呼び出されず、READMEの「依存関係グラフによるインクリメンタル再ビルド」という主張は現状では構想段階にとどまっています。  
- **バッチ式コンパイル 対 ストリーミング式コンパイル：** `streaming::compile_batch` モジュール（`src/core/streaming.rs`）は、真の意味ではストリーミングしていません。実際には、ページを一時ディレクトリへとバッチ単位でコンパイルし、バッチごとに `staticdatagen::compile` をゼロから実行して出力をマージしています。これは相当なディスクI/Oのオーバーヘッドと冗長な解析を生み、真のストリーミングアーキテクチャから逸脱しています。  
- **プラグインのライフサイクルフェーズ違反：** ビルド過程で新しいHTMLページを生成するプラグイン、たとえば `TaxonomyPlugin`、`PaginationPlugin`、`I18nPlugin` は、`transform_html` のライフサイクルを使わず、`after_compile` で直接ディスクに書き込みます。その結果、これらのプラグインが生成したページは、（`CanonicalPlugin`、`JsonLdPlugin`、`RobotsPlugin`、`AccessibilityPlugin` などの）重要な後処理プラグインが先に登録されていた場合、それらを迂回してしまいます。これにより、タグ、カテゴリー、ページネーションされたページは、正しいカノニカルリンク、JSON-LDスキーマ、アクセシビリティ検証を欠いたまま残ります。  
- **`LlmPlugin` における `curl` へのシェルアウト：** ローカルLLMコンテンツパイプライン（`src/plugins/llm.rs`）は、ローカルエンドポイントへの問い合わせにホストの `curl` バイナリへ直接シェルアウトします。これは深刻なクロスプラットフォームのバグ（たとえばPATHに curl のないWindowsホスト）を引き起こし、セキュリティリスク（シェルインジェクションの経路）をもたらし、ロックダウンされた、あるいはネットワーク隔離されたCI環境では失敗します。  
- **HTMLリライトにおける素朴な文字列操作：** `image_plugin.rs` と `search.rs` の抽出処理は、脆い `str::find` および `str::rfind` の操作でHTML文字列を書き換えます。この手法は、壊れたHTMLタグ、コメント内の `<img>` タグ、alt テキスト内の文字実体参照、既存の `srcset` プロパティなどに対して極めて脆弱であり、出力の破損につながりかねません。  
- **未実装のAVIFサポート：** AVIF画像エンコーディングは詳しくドキュメント化されているものの、`image_plugin.rs` の実装は `avif_variants` が単に `Vec::new()` を返すだけのスタブであり、この機能は動作しないままです。  
- **ポーリングベースのウォッチャー：** ローカル開発サーバーのウォッチャー（`src/server/watch.rs`）は、ファイルシステムのイベントAPIではなくポーリングを使用しており、アイドル時の過剰なCPU使用と、1秒未満の変更検知の遅延を招いています。

### 機能上および開発者体験上のギャップ

- **推移的依存関係の追跡なし：** 依存関係グラフは、ネストした依存関係（たとえば、レイアウトに影響しページに影響するサブテンプレートの変更）を追跡できません。これはユニットテスト `transitive_not_tracked` で確認されています。  
- **インクリメンタルコンパイルのCLIフラグなし：** 実行コンパイラに接続された `--incremental` のCLIフラグが存在せず、開発者はキャッシュされたビルドを利用できません。  
- **HMRはCSSに限定：** ホットモジュールリプレースメント（HMR）はCSSのみをサポートします。HTML、レイアウト、Markdownファイルへの変更はすべてページの完全な再読み込みを引き起こし、開発速度を損ないます。  
- **サブコマンドの不足：** `ssg dev`、`ssg build`、`ssg check`、`ssg lint` といった標準的なサブコマンドが存在しないため、開発者は冗長なフラグ（`ssg -s public -w`）を手動で渡さなければなりません。

---

## 私たちに欠けているアーキテクチャ上のギャップ（新たな発見）

v0.0.41のギャップを超えて、金融グレードのリスクプロファイルに照らしてプロジェクトを評価すると、まだ提供されていないものの、エンタープライズの購入者が求めるであろういくつかの機能が浮かび上がります。

### 1. WebAssemblyプラグインのサンドボックス化（ゼロトラスト拡張）

コンパイラのバイナリ自体は安全なRustで書かれていますが、任意のサードパーティ製プラグインがホストシステム上でネイティブに実行されることを許すと、深刻なサプライチェーンの脆弱性が生じます。侵害されたサードパーティ製プラグインは、ホストのファイルシステムに容易にアクセスし、専有のMarkdownファイルを読み取り、あるいは非公開の認証情報を持ち出しかねません。

* **欠けている機能：** サンドボックス化された実行環境。ゼロトラストのコンパイルを実現するには、コンパイラはサードパーティ製プラグインを組み込みのWebAssemblyランタイム（`wasmtime` など）の内部で実行すべきです。プラグインは、制限されたWebAssemblyシステムインターフェース（WASI）を介してのみホストとやり取りし、そのアクセスを変換対象のページに厳格に限定すべきです。

### 2. ストリーミングASTによるゼロコピーHTML解析（`lol_html`）

HTML解析レイヤーを（Kuchikiやhtml5everのような）完全なインメモリDOMライブラリへ移行すると、100,000ページを超えるサイトを扱う際に、相当なメモリのオーバーヘッドと処理の停止が生じます。

* **欠けている機能：** ストリーミング型のゼロコピーHTMLリライター。Cloudflareの `lol_html`（Low-Output-Latency HTMLリライター）を活用すれば、コンパイラはほぼゼロのメモリ割り当てで、単一のストリーミングパスによりHTML要素を解析、検査、変更でき、並列ストリーミングコンパイラが目標とする1秒未満のビルドに合致します。

### 3. ローカルセマンティックベクトル検索（ローカルRAG）

現在の検索インデックス（`SearchPlugin`）は、重くフラットなJSONインデックスを生成し、単純なクライアント側の文字列一致を行うにとどまり、あいまい検索、語幹処理、セマンティッククエリをサポートしていません。Pagefindは改善ではありますが、それでも大きなインデックスのダウンロードに依存します。

* **欠けている機能：** 組み込みのセマンティック検索。コンパイラは、ビルド時にローカルで軽量なRustネイティブのベクトル埋め込みモデル（`candle` または `ort` / ONNX Runtime 経由で実行されるMiniLM-L6モデルなど）を活用すべきです。ページの各段落について密なベクトル埋め込みを生成し、コンパクトなベクトルインデックスを出力すべきです。WASMにコンパイルされたクライアント側の検索ウィジェットは、その上でブラウザ内において真のオフラインセマンティック検索を直接実行できます。

### 4. 決定論的な翻訳・推論キャッシュ

ローカルLLMの推論（たとえばOllamaやLlama.cpp経由）はCPU/GPUを非常に多く消費するため、ビルドのたびに数千ページの翻訳やメタデータ生成を行うのは計算上、現実的ではありません。

* **欠けている機能：** コンテンツハッシュに基づく推論キャッシュ。コンパイラは、すべてのLLM操作の決定論的なキャッシュを維持しなければなりません。あるMarkdownファイルの内容とその翻訳パラメーターのSHA-256ハッシュがキャッシュエントリと一致する場合、コンパイラはキャッシュされた翻訳とメタデータを再利用し、冗長なローカル推論を回避すべきです。

### 5. 並列スケーリングのための非同期ファイルI/O

プラグインパイプラインはRayonによって並列化されていますが、標準的な同期的ディスク書き込みはRayonのOSスレッドをブロックし、数万ページを書き込む際にI/Oのボトルネックを生みます。

* **欠けている機能：** 非同期でノンブロッキングなディスクI/O。コンパイラは、CPU集約的なタスク（Markdownの解析、圧縮）をディスク依存の書き込みから切り離し、非同期I/Oスレッドプールまたはlinuxの `io_uring` バインディング（`rio` または `tokio` 経由）を用いて、並列CPUエグゼキューターをブロックすることなく、コンパイル済みのページを並列に書き込むべきです。

---

## 戦略的な1.0ロードマップ

以下のロードマップは、解消済みのギャップと、新たに発見されたエンタープライズグレードの機能の双方を、構造化された時系列のリリースフレームワークへと統合したものです。

### フェーズ1：0.0.42（堅牢性と正確性のパッチ、1～2週間）

1. **`MinifyPlugin` の再構築：** ネイティブで構文を認識したHTML、JS、CSSの圧縮のために、`minify-html`、`oxc_minifier`、`lightningcss` を統合します。プラグインが `site_dir` 配下のネストしたディレクトリをすべて再帰的にたどることを保証します。  
2. **AIパイプラインの安全化：** クロスプラットフォームの互換性を確保しシェルインジェクションの脆弱性を排除するため、`LlmPlugin` をネイティブな `curl` へのシェルアウトから `ureq`（軽量で同期的、安全なRust製HTTPクライアント）へ移植します。  
3. **AVIF実装の完成：** `ravif` を画像アセットパイプラインに直接組み込み、WebPおよびPNGと並んで高性能なAVIFエンコーディングを可能にします。  
4. **HrefLangと多言語ロケールのマッピングの自動化：** 多言語ビルドにおいて並行して翻訳されたページを自動的に検出し、標準的でGoogle準拠の `<link rel="alternate" hreflang="..." />` タグを、コンパイルされた各HTMLファイルの head へ注入します。  
5. **JSON Feed 1.1のサポート：** 標準的なRSS 2.0およびAtom 1.0の配信チャネルと並んで、専用のJSON Feed 1.1エミッターを提供します。

### フェーズ2：0.1.0（信頼性とインクリメンタルのマイナー版、2～3か月）

1. **`DepGraph` の投入と `--incremental` の有効化：** `DepGraph` を完全に接続し、テンプレートからページへ、Markdownからページへの依存関係を追跡します。キャッシュ無効化レイヤーを実装し、`--incremental` のCLIフラグを接続して、ウォームキャッシュ環境で200ミリ秒未満の再ビルドを目標とします。  
2. **`lol_html` によるストリーミングASTリライト：** `image_plugin.rs`、`search.rs`、CSP注入における脆い文字列書き換えを、`lol_html` によって駆動されるストリーミング型のゼロコピーHTMLリライターへ置き換えます。  
3. **イベント駆動ウォッチャーとコンポーネントHMR：** watchモジュールをポーリングからイベント駆動の `notify` クレートへ移植し、100ミリ秒未満のブラウザ更新を実現するCSSのみおよび部分的HTMLのホットリロードを実装します。  
4. **統一コマンドCLI：** コンパイラのインターフェースを再設計し、標準的なサブコマンドをサポートします。すなわち `ssg dev`、`ssg build`、`ssg check`（アクセシビリティ/SEO監査）、`ssg deploy` です。  
5. **決定論的な推論キャッシュ：** すべてのローカルLLMによる翻訳、要約、メタデータ抽出タスクに対して、コンテンツハッシュに基づくキャッシュレイヤーを実装します。

### フェーズ3：1.0.0（エンタープライズおよび本番向けメジャー版、6～12か月）

1. **ゼロトラストWASMプラグインのサンドボックス化：** WebAssemblyランタイム（`wasmtime` または `wasmer`）を組み込み、ケイパビリティベースのファイルシステムおよびネットワークアクセスを備えた完全にサンドボックス化された環境で、サードパーティ製プラグインを実行します。  
2. **ローカルセマンティックベクトル検索（ローカルRAG）：** ローカルなRustネイティブの埋め込みモデル（`candle` または `ort` 経由）を組み込み、密な段落埋め込みをコンパクトなインデックスへとコンパイルし、プライベートでクライアント側のセマンティック検索を可能にします。  
3. **サーバーアイランドとWASMエッジターゲット：** コンパイルされた `ssg-wasm` コアの上に構築された、エッジランタイム（Cloudflare Workers、Vercel Edge、Netlify Edge など）上での `<ssg-island>` コンポーネントの実行を実装します。  
4. **非同期並列I/Oエンジン：** ファイルシステム書き込みモジュールを再設計し、非同期I/Oスレッドプールと `io_uring` バインディングを用いることで、並列書き込み時のCPUワーカーのブロックを解消します。  
5. **SLSA v1.1ビルドプロベナンスとSPDX 3.0準拠：** 数学的に検証可能なSLSAレベル3のビルドプロベナンスを提供し、SPDX 3.0準拠のSBOMを生成することで、現代のソフトウェアサプライチェーンセキュリティ標準を完全に満たします。

---

## 競合比較マトリクス（2026年の動向）

以下のマトリクスは、`static-site-generator`（v1.0の目標）を、2026年の主要なWeb公開エンジンと比較したものです。

| 機能 | static-site-generator v1.0 | Hugo v0.155+ | Zola v0.19+ | Astro 5 | Eleventy 3 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| **言語 / ランタイム** | Rust（Zero Unsafe） | Go | Rust | JS (Node/V8) | JS (Node/V8) |
| **A11yビルドゲート** | ビルド時AST検証 | なし | なし | ビルド後Linter | ビルド後Linter |
| **セキュリティ強化** | SHA-384 SRIおよびCSP注入 | 手動 | 手動 | 手動 | 手動 |
| **サプライチェーンの安全性** | SLSA L3 \+ SPDX 3.0 \+ WASMサンドボックス | 最小限 | 最小限 | 重いNPMツリー | 重いNPMツリー |
| **AIコンテンツパイプライン** | プライベート、ローカルファースト（ローカルLLM） | なし | なし | パブリックAPIのみ | パブリックAPIのみ |
| **インクリメンタル速度** | \<200ms（ウォームキャッシュ） | \<100ms | \<150ms | \~1.5s | \~140ms |
| **動的インタラクティビティ** | サーバーアイランド（WASMターゲット） | なし | なし | サーバーアイランド（JS） | アイランド（JS） |
| **検索エンジン** | ローカルセマンティックWASM検索 | 単純な文字列 | 単純な文字列 | Pagefind (JS) | Pagefind (JS) |

---

## 1.0におけるポジショニング

1.0において意図されているポジショニングは、セキュア・バイ・デフォルトのソフトウェアインフラとして設計された静的サイトジェネレーターです。すなわち、ローカルファーストのAIパイプラインに支えられた執筆、並列ストリーミングパイプラインによる100,000ページ超のコンパイル、ビルドゲートとして強制されるWCAG 2.2 AAおよび厳格なCSPとSRI、そしてサンドボックス化された動的アイランドであり、これらすべてが単一のメモリ安全なRustバイナリの中に収まります。この記述の各節は、マーケティング上の願望ではなく、上記ロードマップの具体的な項目に対応しています。

---

## 規制およびコンプライアンスの統合

高いリスクを伴うエンタープライズおよび金融の各分野では、ソフトウェアはコンプライアンスとリスク資本の観点から評価されます。`static-site-generator` のアーキテクチャロードマップは、主要な規制上の要請と直接的に整合します。

- **DORA第6条（ICTリスク管理）：** SHA-384 SRIハッシュと厳格なContent Security Policyのコンパイル時の算出と注入は、デジタル公開チャネルをサプライチェーン注入、Web改ざん、クロスサイトスクリプティング（XSS）の経路から保護するという要件を満たします。  
- **DORA第7条（ICTシステムの耐障害性）：** 不変でコンパイル時に検証された静的アセットへ移行することで、金融機関はデータベースおよびランタイムサーバーの脆弱性を排除し、運用リスク乗数を下げ、バーゼルIIIのもとで求められるリスク資本の準備額を削減します。  
- **欧州アクセシビリティ法（EAA）指令 (EU) 2019/882：** アクセシビリティ監査を確実なコンパイラゲートとしてコンパイルパイプラインへと前倒し（シフトレフト）することで、デプロイ前に100%の準拠を保証し、EAAおよびADA第III編のもとでのブランド毀損と民事訴訟のリスクを排除します。  
- **GDPR第25条（プライバシー・バイ・デザイン）：** 翻訳とメタデータのパイプラインをローカルでネットワーク隔離されたハードウェア上で実行することで、専有の草稿、財務指標、個人データをパブリックなサードパーティのクラウドLLMプロバイダーの外に保ち、データ主権の原則への準拠を支えます。

---

## よくある質問

**バージョン0.0.41は、READMEの主張に対して、実際に今日何を提供しているのですか?**
セキュリティとアクセシビリティのモデルは実在し、コードとして強制されています。すなわち、ワークスペース全体での `forbid(unsafe_code)`、SHA-256/384のSRI生成、CSP抽出、Sigstore証跡を伴う署名付きリリースとCycloneDX SBOM、そしてビルドを停止させるWCAG 2.2 AAゲートです。ドキュメント化された機能のうち3つはv0.0.41では機能していません。`MinifyPlugin` は構文を認識する圧縮機能ではなく空白を詰めるだけのものであり、インクリメンタル再ビルドを駆動するはずの `DepGraph` はコンパイルされるものの本番コードでは一度も投入されず、AVIFエンコーディングは `avif_variants` が空のベクターを返すスタブです。

**アクセシビリティゲートは、真のコンパイラゲートですか、それともビルド後のLinterですか?**
それはビルドゲートです。WCAG 2.2 AAのチェックは、Playwrightによって駆動されるビルド時のaxe-coreパーサーを介してコンパイルパイプライン内で実行され、失敗したページは事後に警告を出すのではなく、正確な行番号のエラーとともにコンパイルを停止させます。これこそ、欧州アクセシビリティ法の義務が求める性質です。すなわち、準拠していない出力はデプロイに到達できないということです。

**LLMプラグインにおける `curl` へのシェルアウトは、なぜ問題なのですか?**
ローカルLLMパイプライン（`src/plugins/llm.rs`）は、ローカルエンドポイントに到達するためにホストの `curl` バイナリを呼び出します。これはビルドをホストの実行ファイルに結びつけ、PATHに `curl` のないシステムでは失敗し、シェルインジェクションの攻撃面をもたらし、ネットワーク隔離されたCIでは破綻します。この呼び出しを `ureq` のようなRust製HTTPクライアントへ移植すれば、外部依存とインジェクションの経路が取り除かれます。これが、0.0.42のパッチにおける2番目の項目である理由です。

**1.0への道において、最も重要な単一の項目は何ですか?**
`DepGraph` の投入と `--incremental` フラグの接続です。インクリメンタル再ビルドは、ドキュメント上のエンジンと実際のエンジンとの間の信頼性のギャップであり、100,000ページ超における1秒未満のビルドという後続のあらゆる主張は、依存関係グラフがテスト専用の基盤にとどまるのではなく、テンプレートからページへ、Markdownからページへのエッジを追跡することに依存しています。

## 参考文献

- [Cloudflare, *lol-html: Low-Output-Latency ストリーミングHTMLリライター*](https://github.com/cloudflare/lol-html "Cloudflare lol-html — ストリーミングHTMLリライター") ⧉。［0.1.0フェーズで脆い文字列操作を置き換えるべく提案された、ストリーミング型のゼロコピーHTMLリライター。］
- [W3C, *Webコンテンツアクセシビリティ・ガイドライン（WCAG）2.2*](https://www.w3.org/TR/WCAG22/ "W3C — WCAG 2.2 勧告") ⧉。［コンパイル時のアクセシビリティゲートによって強制されるレベルAAの達成基準。］
- [欧州連合, *規則 (EU) 2022/2554（DORA）*](https://eur-lex.europa.eu/eli/reg/2022/2554/oj "EUR-Lex — デジタルオペレーショナルレジリエンス法") ⧉。［本セキュリティ姿勢が対応するICTリスク管理および耐障害性の条項。］
- [OpenSSF, *Supply-chain Levels for Software Artifacts（SLSA）v1.0*](https://slsa.dev/spec/v1.0/ "SLSA — v1.0 仕様") ⧉。［1.0で検証可能なレベル3の証跡を目標とするビルドプロベナンスのフレームワーク。］
- [Armin Ronacher, *MiniJinjaテンプレートエンジン*](https://github.com/mitsuhiko/minijinja "MiniJinja — Rust向けの最小限のJinja2エンジン") ⧉。［Teraを置き換え推移的ツリーを削減した、依存関係の軽いエンジン。］
- [CycloneDX, *ソフトウェア部品表仕様 v1.5*](https://cyclonedx.org/docs/1.5/ "CycloneDX — SBOM仕様 v1.5") ⧉。［サプライチェーン監査のためにビルドごとに生成されるSBOM形式。］
- [欧州連合, *指令 (EU) 2019/882（欧州アクセシビリティ法）*](https://eur-lex.europa.eu/eli/dir/2019/882/oj "EUR-Lex — 欧州アクセシビリティ法") ⧉。［ビルド時のWCAGゲートが満たすべく設計されているアクセシビリティ義務。］

*最終レビュー：2026年7月。当初の分析はv0.0.41時点の `static-site-generator` コードベースの検査に基づきます。出典は引用であり、複製ではありません。バージョン番号と機能の状況は急速に変化するため、再公開前にリポジトリと照合してください。CC-BY-4.0のもとでライセンスされています。*

