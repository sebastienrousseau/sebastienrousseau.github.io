---
author: "contact@sebastienrousseau.com (Sebastien Rousseau)"
banner_alt: "רקע טכני מופשט המייצג את מפת הדרכים הארכיטקטונית של מחולל אתרים סטטיים ברמת ארגון."
banner_height: "1280"
banner_width: "1920"
banner: "https://cloudcdn.pro/stocks/images/gemini-background.webp"
cdn: "https://cloudcdn.pro"
charset: "UTF-8"
cname: "sebastienrousseau.com"
copyright: "© Copyright 2025 - 2026 - Sebastien Rousseau. All rights reserved."
date: "July 22, 2026"
description: "צלילה לעומק אל מחולל אתרים סטטיים הכתוב ב־Rust: אבטחה בזמן קומפילציה, שערי WCAG ובינה מלאכותית מקומית, הפערים בגרסה 0.0.41 ומפת דרכים ארגונית אל 1.0."
format-detection: "telephone=no"
hreflang: "he"
icon: "https://cloudcdn.pro/clients/sebastienrousseau/v1/logos/sebastienrousseau.svg"
id: "https://sebastienrousseau.com/he/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
image_alt: "דיוקן בשחור־לבן של סבסטיאן רוסו"
image_height: "162"
image_width: "162"
image: "https://cloudcdn.pro/stocks/images/sebastienrousseau.webp"
keywords: "מחולל אתרים סטטיים, Rust, forbid unsafe_code, WCAG 2.2 AA, Content Security Policy, Subresource Integrity, SLSA, CycloneDX SBOM, צנרת LLM מקומית, DORA, בנייה מצטברת, lol_html, ארגז חול לתוספי WASM, חיפוש וקטורי סמנטי, MiniJinja, Ollama"
language: "he"
last_reviewed: "2026-07-22"
layout: "report"
locale: "he_IL"
logo_alt: "הלוגו של סבסטיאן רוסו"
logo_height: "44"
logo_width: "44"
logo: "https://cloudcdn.pro/clients/sebastienrousseau/v1/logos/sebastienrousseau.svg"
menu: ""
measurementID: "G-169G4ET5HQ"
name: "Sebastien Rousseau"
permalink: "https://sebastienrousseau.com/he/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
rating: "general"
referrer: "no-referrer"
robots: "index, follow"
schema: "FAQPage, Article"
seo_title: "מחולל אתרים סטטיים: הדרך אל גרסה 1.0"
short_name: "sebastienrousseau"
subtitle: "ביקורת ארכיטקטונית ומפת דרכים למחולל אתרים סטטיים הכתוב ב־Rust ובנוי כתשתית מאובטחת כברירת מחדל: מה גרסה 0.0.41 באמת מספקת לעומת מה שה־README מבטיח, חמש יכולות ארגוניות חסרות, ומסלול מדורג אל גרסה 1.0 המתואמת עם DORA וחוק הנגישות האירופי."
tags: "מחולל אתרים סטטיים, Rust, אבטחת רשת, נגישות, DORA, שרשרת אספקה, SLSA, בינה מלאכותית מקומית, WCAG, זמן קומפילציה, מפת דרכים, ארגוני"
theme-color: "0, 83, 191"
title: "מחולל אתרים סטטיים (SSG): צלילה אסטרטגית ומפת דרכים ארכיטקטונית"
url: "https://sebastienrousseau.com/he/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
viewport: "width=device-width, initial-scale=1, shrink-to-fit=no"
atom_link: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap/rss.xml"
category: "Technology"
docs: "https://validator.w3.org/feed/docs/rss2.html"
generator: "Static Site Generator (SSG) (version 0.0.26)"
item_description: "צלילה לעומק אל מחולל אתרים סטטיים הכתוב ב־Rust: אבטחה בזמן קומפילציה, שערי WCAG ובינה מלאכותית מקומית, הפערים בגרסה 0.0.41 ומפת דרכים ארגונית אל 1.0."
item_guid: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap/rss.xml"
item_link: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap/rss.xml"
item_pub_date: "Wed, 22 Jul 2026 07:07:07 +0000"
item_title: "מחולל אתרים סטטיים (SSG): צלילה אסטרטגית ומפת דרכים ארכיטקטונית"
last_build_date: "Wed, 22 Jul 2026 06:06:06 +0000"
managing_editor: "contact@sebastienrousseau.com (Sebastien Rousseau)"
pub_date: "Wed, 22 Jul 2026 07:07:07 +0000"
ttl: "60"
type: "article"
webmaster: "contact@sebastienrousseau.com"
apple_mobile_web_app_orientations: "portrait"
apple_touch_icon_sizes: "192x192"
apple-mobile-web-app-capable: "yes"
apple-mobile-web-app-status-bar-inset: "black"
apple-mobile-web-app-status-bar-style: "black-translucent"
apple-mobile-web-app-title: "SSG: הדרך אל גרסה 1.0"
apple-touch-fullscreen: "yes"
msapplication-navbutton-color: "0, 83, 191"
twitter_card: "summary_large_image"
twitter_creator: "@wwdseb"
twitter_description: "שערי WCAG בזמן קומפילציה, SRI מסוג SHA-384, הזרקת CSP וצנרת LLM מקומית מייחדים מנוע Rust זה. ניתוח פערים כן של גרסה 0.0.41 והדרך אל 1.0 ברמת ארגון."
twitter_image: "https://cloudcdn.pro/clients/sebastienrousseau/v1/logos/sebastienrousseau.svg"
twitter_image_alt: "הלוגו של סבסטיאן רוסו"
twitter_site: "@wwdseb"
twitter_title: "מחולל אתרים סטטיים: הדרך אל גרסה 1.0"
twitter_url: "https://sebastienrousseau.com/2026-07-22-ssg-enterprise-strategic-deep-dive-architectural-roadmap"
excerpt: "שערי WCAG בזמן קומפילציה, SRI מסוג SHA-384 והזרקת CSP מייחדים מנוע Rust זה, אך בנייה מצטברת ומזעור נייטיב עדיין שאיפתיים. הנה ניתוח פערים כן והדרך אל 1.0."
author_website: "https://sebastienrousseau.com"
author_twitter: "@wwdseb"
author_location: "London, UK"
thanks: "תודה שקראתם!"
site_last_updated: "2026-07-22"
site_standards: "HTML5, CSS3, RSS, Atom, JSON, XML, YAML, Markdown, TOML"
site_components: "Kaishi, Kaishi Builder, Kaishi CLI, Kaishi Templates, Kaishi Themes"
---

# מחולל אתרים סטטיים (SSG): צלילה אסטרטגית לעומק ומפת דרכים ארכיטקטונית ברמת ארגון

*תאריך המחקר: 2026-06-22. מבוסס על בדיקת בסיס הקוד של `static-site-generator` בגרסה v0.0.41 ועל מחקר רשת של נוף מחוללי האתרים הסטטיים לשנת 2026.*

**עבור מפרסם מפוקח, מחולל אתרים סטטיים אינו עוד כלי עיצוב; הוא חלק מהיקף הסיכון התפעולי.** ה־[static-site-generator](https://github.com/sebastienrousseau/static-site-generator) בקוד פתוח, הכתוב ב־Rust, בנוי על הנחה זו: הוא מעביר את האבטחה, הנגישות, ההתאמה הבינלאומית ותהליכי תוכן ה־AI לשלב הקומפילציה, כך שבדיקה כושלת עוצרת את הבנייה במקום להגיע לסביבת הייצור. ניתוח זה מפריד בין מה שגרסה 0.0.41 אכן מספקת לבין מה שהתיעוד שלה עדיין רק מבטיח, מציג חמש יכולות ארגוניות שטרם קיימות בה, ומציע מסלול מדורג לשחרור גרסה 1.0 המתואם עם DORA, חוק הנגישות האירופי ותקני שרשרת האספקה המודרניים.

<!-- lead-start -->
<aside class="post-lead" aria-label="תקציר המאמר">
<p class="post-lead-tldr"><strong>לקצרים בזמן.</strong> ה־<code>static-site-generator</code> הכתוב ב־Rust מתייחס לפרסום ברשת כאל צנרת תוכנה ברת־ביקורת ומאובטחת כברירת מחדל: <code>forbid(unsafe_code)</code> בכל סביבת העבודה, שלמות משאבי משנה (Subresource Integrity) מסוג SHA-384, חילוץ מדיניות אבטחת תוכן (Content Security Policy), שער WCAG 2.2 AA בזמן קומפילציה וצנרת LLM מקומית תחילה. בדיקת קוד של גרסה 0.0.41 מראה שכמה תכונות מתועדות עדיין שאיפתיות, וביניהן מזעור נייטיב, בנייה מצטברת ו־AVIF. זהו ניתוח פערים כן ומפת דרכים מדורגת אל גרסה 1.0 ברמת ארגון.</p>
<p class="post-lead-heading"><strong>עיקרי הדברים</strong></p>
<ul class="post-lead-takeaways">
  <li><strong>מודל האבטחה והנגישות ממשי.</strong> SRI בזמן קומפילציה, חילוץ CSP, שחרורים חתומים עם אישור Sigstore ורשימות רכיבים (SBOM) מסוג CycloneDX, וכן שער WCAG 2.2 AA העוצר את הבנייה, מיושמים בקוד ולא רק מתועדים.</li>
  <li><strong>כמה תכונות מרכזיות אינן ממשיות.</strong> המזער הוא מכווץ רווחים לבנים, גרף התלות שאמור להניע בנייה מצטברת אינו מאוכלס לעולם בסביבת הייצור, וקידוד AVIF הוא גדם המחזיר וקטור ריק.</li>
  <li><strong>חמש יכולות ארגוניות חסרות.</strong> ארגז חול לתוספי WASM, כותב־מחדש HTML זורם ללא העתקה, חיפוש סמנטי מקומי, מטמון היקש דטרמיניסטי וקלט/פלט קבצים א־סינכרוני.</li>
  <li><strong>מפת הדרכים מסודרת לפי סיכון.</strong> תיקון תקינות (0.0.42), גרסת משנה של אמינות ובנייה מצטברת (0.1.0), ולאחריה גרסת מייג'ור ארגונית (1.0.0) הנושאת את ארגז החול, החיפוש הסמנטי וייחוס SLSA v1.1.</li>
</ul>
<p class="post-lead-related"><strong>קריאה נוספת:</strong> <a href="https://sebastienrousseau.com/2026-07-03-emerging-technology-risk-horizon-banks-2026">אופק סיכוני הטכנולוגיה המתפתחת עבור בנקים</a>, <a href="https://sebastienrousseau.com/2026-07-07-corporate-banking-api-standard-agentic-mcp-2026">תקן API לבנקאות תאגידית עבור MCP סוכני</a>.</p>
</aside>
<!-- lead-end -->

> **תקציר מנהלים**
>
> - **הפרסום הוא כיום היקף סיכון תפעולי.** תחת DORA, חוק הנגישות האירופי ו־GDPR, כל נכס פונה־ציבור הוא נקודת כניסה אפשרית לפריצת שרשרת אספקה, השחתת אתר וחשיפה רגולטורית. מודל של זמן קומפילציה מצמצם את ההיקף הזה בכך שהוא דוחה פלט לא־תואם לפני שהוא נשלח.
> - **הבידול של המנוע נאכף על ידי המהדר, ולא שאיפות מתועדות.** `forbid(unsafe_code)` בכל סביבת העבודה, SRI אמיתי מסוג SHA-256/384, חילוץ CSP אוטומטי ושער WCAG 2.2 AA בזמן בנייה הופכים את האבטחה והנגישות מביקורות בדיעבד לכשלי בנייה קשיחים.
> - **בגרסה 0.0.41 יש פער בין התיעוד לקוד.** מזעור נייטיב, בנייה מצטברת באמצעות גרף תלות ותמיכה ב־AVIF מתוארים אך אינם מתפקדים; המאמר מציין כל פער מול מיקום המקור המדויק.
> - **הדרך אל 1.0 היא רצף, לא רשימת משאלות.** תחילה חוסן (0.0.42), אחריו תקינות מצטברת (0.1.0), ולבסוף היכולות הארגוניות — ארגז חול WASM, חיפוש סמנטי מקומי וייחוס SLSA בר־אימות — שקונה מפוקח דורש (1.0.0).

## חוזקות נוכחיות

בסיס הקוד של `static-site-generator` מגלם כמה החלטות הנדסיות ייחודיות המבדילות אותו ממנועי JavaScript ו־Go מדור קודם:

- **תנוחת אבטחה בזמן קומפילציה:** `#![forbid(unsafe_code)]` בכל סביבת העבודה מספק ערובות בטיחות־זיכרון בזמן קומפילציה. צנרת הבנייה מייצרת גיבובי שלמות משאבי משנה (SRI) אמיתיים מסוג SHA-256/SHA-384 (`src/plugins/assets.rs`) ומבצעת חילוץ אוטומטי של מדיניות אבטחת תוכן (CSP) המסיר סקריפטים וסגנונות מסוג unsafe-inline. השחרורים חתומים, נושאים אישור Sigstore ומפיקים SBOM מסוג CycloneDX 1.5 בכל בנייה.  
- **שער נגישות הנאכף על ידי המהדר:** בדיקות הנחיות הנגישות לתוכן ברשת (WCAG) 2.2 ברמה AA רצות בתוך צנרת הקומפילציה באמצעות מנתח axe-core בזמן בנייה המונע על ידי Playwright. הנגישות הופכת לשער בנייה קשיח ולא לביקורת שלאחר הפרסום: אם עמוד נכשל, הקומפילציה נעצרת עם שגיאות המציינות מספרי שורה מדויקים.  
- **צנרת AI ריבונית־נתונים:** צנרת מקומית של LLM לתרגום וחילוץ מטא־נתונים (דרך נקודות קצה מקומיות של Ollama או llama.cpp) מאפשרת למוסד להפוך לאוטומטי את תמצות התוכן, יצירת סכמת JSON-LD ותרגום רב־לשוני, מבלי לשלוח גילויים טרום־דוחות או קניין רוחני רגיש אל ממשקי AI ציבוריים בענן.  
- **קומפילציה מקבילית:** ערובות בטיחות־הזיכרון של Rust מהוות בסיס לצנרת HTML ונכסים מקבילית מונעת־Rayon (`src/core/pipeline.rs`). צנרת התוספים מבצעת התמרות ממוזגות, כאשר `SearchPlugin`, `SeoPlugin`, `CanonicalPlugin` ו־`JsonLdPlugin` פועלים מעל `par_iter()`, כך שכל עמוד נקרא ונכתב לדיסק פעם אחת.  
- **היגיינת שרשרת אספקה ותלויות:** המעבר של מנוע התבניות מ־Tera ל־MiniJinja (`v0.0.37`) הקטין את גודל הבינארי, הסיר תלויות מעבר כגון `rand` בזמן קומפילציה, והפיק טביעת רגל תלויות קומפקטית המפחיתה את החשיפה לשרשרת אספקת התוכנה.

---

## פערים ומציאות בשטח

על אף חוזקות יוצאות דופן אלה, בדיקה קפדנית של בסיס הקוד בגרסה 0.0.41 חושפת כמה פערים ארכיטקטוניים, פונקציונליים וחווייתיים למפתחים בין טענות התיעוד לבין קוד ה־Rust בפועל:

### פערים ארכיטקטוניים

- **כיווץ רווחים לבנים לעומת מזעור נייטיב:** בעוד ה־README מבטיח "מזעור JS/CSS נייטיב", ה־`MinifyPlugin` (`src/plugins/plugins.rs:96-116`) פועל אך ורק ככווץ רווחים לבנים תמים. הוא עוקף אלמנטי `<pre>` ומכווץ רצפי רווחים לבנים ב־HTML, אך אינו מבצע מזעור נייטיב מודע־תחביר של CSS או JS. יתרה מכך, הוא מעבד רק עמודים ברמה העליונה ואינו סורק רקורסיבית תיקיות משנה (כגון `/blog/` או `/tags/`), ומותיר עמודים עמוקים ללא מזעור.  
- **תשתית מצטברת מתה:** גרף מעקב התלות (`DepGraph` ב־`src/core/depgraph.rs`) עובר קומפילציה ונטען אל `PluginContext.dep_graph`, אך אינו מאוכלס בפועל בקוד הייצור. המתודה `add_dep()` נקראת רק בבדיקות יחידה, מה שהופך את טענת ה־README על "בנייה מצטברת באמצעות גרפי תלות" לשאיפתית כרגע.  
- **קומפילציה באצוות לעומת קומפילציה זורמת:** המודול `streaming::compile_batch` (`src/core/streaming.rs`) אינו זורם באמת. במקום זאת הוא מהדר עמודים באצוות אל תיקייה זמנית, מריץ `staticdatagen::compile` מאפס עבור כל אצווה, וממזג את הפלטים. הדבר גורם לתקורה משמעותית של קלט/פלט לדיסק ולניתוח מיותר, בסטייה מארכיטקטורה זורמת אמיתית.  
- **הפרות שלב במחזור חיי התוספים:** תוספים המייצרים עמודי HTML חדשים במהלך תהליך הבנייה, כגון `TaxonomyPlugin`, `PaginationPlugin` ו־`I18nPlugin`, כותבים ישירות לדיסק ב־`after_compile` במקום להשתמש במחזור החיים `transform_html`. כתוצאה מכך, עמודים שנוצרו על ידי תוספים אלה עוקפים תוספי עיבוד־בתר קריטיים (כגון `CanonicalPlugin`, `JsonLdPlugin`, `RobotsPlugin` ו־`AccessibilityPlugin`) אם תוספים אלה נרשמו קודם לכן. הדבר מותיר עמודי תגיות, קטגוריות ועמודים בעימוד ללא קישורים קנוניים תקינים, סכמות JSON-LD או אימותי נגישות.  
- **קריאה חיצונית ל־`curl` ב־`LlmPlugin`:** צנרת תוכן ה־LLM המקומית (`src/plugins/llm.rs`) קוראת ישירות לבינארי `curl` של המארח כדי לתשאל נקודות קצה מקומיות. הדבר מכניס באגים חמורים בין־פלטפורמיים (למשל, במארחי Windows שאין בהם curl ב־PATH), מהווה סיכון אבטחה (וקטורי הזרקת שֶׁל), ונכשל בסביבות CI נעולות או מבודדות־רשת.  
- **מניפולציית מחרוזות תמימה בכתיבה־מחדש של HTML:** המחלצים `image_plugin.rs` ו־`search.rs` כותבים מחדש מחרוזות HTML באמצעות פעולות `str::find` ו־`str::rfind` שבירות. גישה זו פגיעה מאוד לתגיות HTML שבורות, לתגיות `<img>` בתוך הערות, לישויות תווים בטקסט חלופי, או למאפייני `srcset` קיימים, מה שעלול לגרום לפלט משובש.  
- **תמיכת AVIF לא ממומשת:** אף שקידוד תמונות AVIF מתועד בהרחבה, המימוש ב־`image_plugin.rs` הוא גדם שבו `avif_variants` פשוט מחזיר `Vec::new()`, ומותיר את התכונה לא־מתפקדת.  
- **צופה מבוסס־תשאול:** הצופה של שרת הפיתוח המקומי (`src/server/watch.rs`) משתמש בתשאול ולא בממשקי אירועי מערכת קבצים, מה שמוביל לשימוש מוגזם ב־CPU בסרק ולהשהיה של שינויים ברמת שנייה.

### פערים פונקציונליים וחווייתיים

- **אין מעקב תלויות מעבר:** גרף התלות אינו יכול לעקוב אחר תלויות מקוננות (למשל, שינויים בתבנית משנה המשפיעה על פריסה המשפיעה על עמוד), כפי שאומת בבדיקת היחידה `transitive_not_tracked`.  
- **אין דגל CLI לקומפילציה מצטברת:** אין דגל CLI מסוג `--incremental` המחובר למהדר הריצה, מה שמונע ממפתחים להשתמש בבנייה ממוטמנת.  
- **HMR מוגבל ל־CSS:** החלפת מודולים חמה (HMR) תומכת רק ב־CSS; כל שינוי בקבצי HTML, פריסות או markdown מפעיל טעינה מחדש מלאה של העמוד, ופוגע במהירות המפתח.  
- **מחסור בתת־פקודות:** מפתחים נאלצים להעביר ידנית דגלים מפורטים (`ssg -s public -w`) משום שתת־פקודות סטנדרטיות כגון `ssg dev`, `ssg build`, `ssg check` ו־`ssg lint` אינן קיימות.

---

## פערים ארכיטקטוניים החסרים לנו (תגליות חדשות)

מעבר לפערים בגרסה 0.0.41, הערכת הפרויקט מול פרופיל סיכון ברמה פיננסית מעלה כמה יכולות שהוא עדיין אינו מספק אך קונה ארגוני היה דורש:

### 1. ארגז חול לתוספי WebAssembly (הרחבת אפס־אמון)

בעוד הבינארי של המהדר עצמו כתוב ב־Rust בטוח, מתן אפשרות לתוספי צד־שלישי שרירותיים לרוץ בצורה נייטיבית על מערכות המארח מכניס פגיעות שרשרת אספקה חמורה. תוסף צד־שלישי שנפרץ יכול בקלות לגשת למערכת הקבצים של המארח, לקרוא קובצי Markdown קנייניים, או לחלץ אישורי גישה פרטיים.

* **יכולת חסרה:** סביבת ריצה בארגז חול. כדי להשיג קומפילציית אפס־אמון, על המהדר להריץ תוספי צד־שלישי בתוך סביבת ריצה מוטמעת של WebAssembly (כגון `wasmtime`). על התוספים לתקשר עם המארח אך ורק דרך ממשק מערכת WebAssembly (WASI) מוגבל, המצמצם את גישתם אך ורק לעמוד המותמר.

### 2. ניתוח HTML ללא העתקה באמצעות AST זורם (`lol_html`)

מעבר שכבת ניתוח ה־HTML לספריית DOM מלאה בזיכרון (כגון Kuchiki או html5ever) מכניס תקורת זיכרון משמעותית והשהיות עיבוד בטיפול באתרים עם למעלה מ־100,000 עמודים.

* **יכולת חסרה:** כותב־מחדש HTML זורם וללא העתקה. שימוש ב־`lol_html` של Cloudflare (כותב־מחדש HTML בהשהיית־פלט נמוכה) מאפשר למהדר לנתח, לבחון ולשנות אלמנטי HTML במעבר זורם יחיד עם הקצאת זיכרון קרובה לאפס, בהתאמה ליעד המהדר הזורם המקבילי של בנייה בפחות משנייה.

### 3. חיפוש וקטורי סמנטי מקומי (RAG מקומי)

מפתח החיפוש הנוכחי (`SearchPlugin`) מייצר מפתח JSON כבד ושטוח המבצע התאמות מחרוזת פשוטות בצד־הלקוח, ללא תמיכה בחיפוש מטושטש, גיזרון או שאילתות סמנטיות. Pagefind הוא שיפור, אך הוא עדיין מסתמך על הורדת מפתח גדול.

* **יכולת חסרה:** חיפוש סמנטי מוטמע. על המהדר לנצל מודל הטמעה וקטורי מקומי, קליל ונייטיב־Rust (כגון מודל MiniLM-L6 המורץ דרך `candle` או `ort` / ONNX Runtime) בזמן בנייה. עליו לייצר הטמעות וקטוריות צפופות עבור כל פסקה בעמוד ולהפיק מפתח וקטורי קומפקטי. יישומון החיפוש בצד־הלקוח, המהודר ל־WASM, יכול אז לבצע חיפוש סמנטי לא־מקוון אמיתי ישירות בדפדפן.

### 4. תרגום דטרמיניסטי ומטמון היקש

מאחר שהיקש LLM מקומי (למשל דרך Ollama או Llama.cpp) עתיר CPU/GPU במידה רבה, תרגום או יצירת מטא־נתונים לאלפי עמודים בכל בנייה הוא בלתי אפשרי מבחינה חישובית.

* **יכולת חסרה:** מטמון היקש מבוסס־גיבוב־תוכן. על המהדר לתחזק מטמון דטרמיניסטי של כל פעולות ה־LLM. אם גיבוב ה־SHA-256 של תוכן קובץ markdown ופרמטרי התרגום שלו תואם רשומת מטמון, על המהדר לעשות שימוש חוזר בתרגום ובמטא־נתונים הממוטמנים, ולעקוף היקש מקומי מיותר.

### 5. קלט/פלט קבצים א־סינכרוני לקנה־מידה מקבילי

בעוד צנרת התוספים מקבילית באמצעות Rayon, כתיבות דיסק סינכרוניות רגילות חוסמות את חוטי מערכת ההפעלה של Rayon, ויוצרות צוואר בקבוק של קלט/פלט בעת כתיבת עשרות אלפי עמודים.

* **יכולת חסרה:** קלט/פלט דיסק א־סינכרוני ולא־חוסם. על המהדר לנתק משימות עתירות־CPU (ניתוח Markdown, מזעור) מכתיבות תלויות־דיסק, באמצעות מאגרי חוטים א־סינכרוניים של קלט/פלט או קישורי `io_uring` של לינוקס (דרך `rio` או `tokio`) כדי לכתוב עמודים מהודרים במקביל מבלי לחסום את מבצעי ה־CPU המקביליים.

---

## מפת הדרכים האסטרטגית אל 1.0

מפת הדרכים הבאה משלבת הן את הפערים שנפתרו והן את היכולות ברמת ארגון שהתגלו לאחרונה למסגרת שחרור מובנית וכרונולוגית.

### שלב 1: 0.0.42 (תיקון החוסן והתקינות, שבוע עד שבועיים)

1. **שחזור `MinifyPlugin`:** שילוב של `minify-html`, `oxc_minifier` ו־`lightningcss` למזעור נייטיב מודע־תחביר של HTML, JS ו־CSS. יש לוודא שהתוסף סורק רקורסיבית את כל תיקיות המשנה המקוננות תחת `site_dir`.  
2. **אבטחת צנרת ה־AI:** מעבר של `LlmPlugin` מקריאות `curl` נייטיביות אל `ureq` (לקוח HTTP קליל, סינכרוני ובטוח ב־Rust) כדי להבטיח תאימות בין־פלטפורמית ולחסל פגיעויות הזרקת שֶׁל.  
3. **השלמת מימוש AVIF:** חיבור `ravif` ישירות אל צנרת נכסי התמונות, המאפשר קידוד AVIF בעל ביצועים גבוהים לצד WebP ו־PNG.  
4. **מיפוי HrefLang ורב־לוקאלים אוטומטי:** זיהוי אוטומטי של עמודים מתורגמים מקבילים בבניות רב־לשוניות והזרקת תגיות `<link rel="alternate" hreflang="..." />` תקניות ותואמות־Google אל כותרת כל קובץ HTML מהודר.  
5. **תמיכת JSON Feed 1.1:** אספקת פולט JSON Feed 1.1 ייעודי לצד ערוצי הפצה סטנדרטיים של RSS 2.0 ו־Atom 1.0.

### שלב 2: 0.1.0 (גרסת המשנה של האמינות והבנייה המצטברת, חודשיים עד שלושה)

1. **אכלוס `DepGraph` והפעלת `--incremental`:** חיבור מלא של `DepGraph` למעקב אחר תלויות תבנית־לעמוד ו־markdown־לעמוד. מימוש שכבת ביטול־תוקף מטמון וחיבור דגל ה־CLI מסוג `--incremental`, במיקוד לבנייה מצטברת בפחות מ־200 ms בסביבות מטמון־חם.  
2. **כתיבה־מחדש של AST זורם באמצעות `lol_html`:** החלפת הכתיבה־מחדש השבירה של מחרוזות ב־`image_plugin.rs`, ב־`search.rs` ובהזרקות CSP בכותב־מחדש HTML זורם וללא העתקה המונע על ידי `lol_html`.  
3. **צופה מונע־אירועים ו־HMR רכיבים:** מעבר מודול הצפייה מתשאול אל ה־crate מונע־האירועים `notify`, ומימוש טעינה חמה של CSS־בלבד ו־HTML־חלקי לעדכוני דפדפן בפחות מ־100 ms.  
4. **CLI פקודות מאוחד:** ארכיטקטורה מחדש של ממשק המהדר לתמיכה בתת־פקודות סטנדרטיות: `ssg dev`, `ssg build`, `ssg check` (ביקורת נגישות/SEO) ו־`ssg deploy`.  
5. **מטמון היקש דטרמיניסטי:** מימוש שכבת מטמון מבוססת־גיבוב־תוכן לכל משימות התרגום, התמצות וחילוץ המטא־נתונים של ה־LLM המקומי.

### שלב 3: 1.0.0 (גרסת המייג'ור הארגונית והייצורית, שישה עד שנים־עשר חודשים)

1. **ארגז חול אפס־אמון לתוספי WASM:** הטמעת סביבת ריצה של WebAssembly (`wasmtime` או `wasmer`) להרצת תוספי צד־שלישי בסביבה מבודדת לחלוטין עם גישה מבוססת־יכולת למערכת הקבצים ולרשת.  
2. **חיפוש וקטורי סמנטי מקומי (RAG מקומי):** הטמעת מודל הטמעה מקומי ונייטיב־Rust (דרך `candle` או `ort`) להידור הטמעות פסקה צפופות למפתח קומפקטי, המאפשר חיפוש סמנטי פרטי בצד־הלקוח.  
3. **איי שרת ויעד קצה WASM:** מימוש הרצת רכיבי `<ssg-island>` בסביבות ריצה בקצה (כגון Cloudflare Workers, Vercel Edge או Netlify Edge) הבנויות מעל ליבת ה־`ssg-wasm` המהודרת.  
4. **מנוע קלט/פלט מקבילי א־סינכרוני:** ארכיטקטורה מחדש של מודול כתיבת מערכת הקבצים לשימוש במאגרי חוטים א־סינכרוניים של קלט/פלט ובקישורי `io_uring`, המחסל חסימות של עובדי CPU במהלך כתיבות מקביליות.  
5. **ייחוס בנייה SLSA v1.1 ותאימות SPDX 3.0:** אספקת ייחוס בנייה SLSA ברמה 3 בר־אימות מתמטי ויצירת SBOM תואמי SPDX 3.0, המספקים במלואם את תקני אבטחת שרשרת אספקת התוכנה המודרניים.

---

## מטריצת מתחרים (נוף 2026)

המטריצה הבאה משווה את `static-site-generator` (יעד v1.0) מול מנועי הפרסום המובילים ברשת לשנת 2026:

| יכולת | static-site-generator v1.0 | Hugo v0.155+ | Zola v0.19+ | Astro 5 | Eleventy 3 |
| :---- | :---- | :---- | :---- | :---- | :---- |
| **שפה / סביבת ריצה** | Rust (Zero Unsafe) | Go | Rust | JS (Node/V8) | JS (Node/V8) |
| **שער בנייה לנגישות** | אימות AST בזמן בנייה | ללא | ללא | לינטר לאחר בנייה | לינטר לאחר בנייה |
| **הקשחת אבטחה** | SHA-384 SRI והזרקת CSP | ידני | ידני | ידני | ידני |
| **בטיחות שרשרת אספקה** | SLSA L3 \+ SPDX 3.0 \+ ארגז חול WASM | מזערי | מזערי | עץ NPM כבד | עץ NPM כבד |
| **צנרת תוכן AI** | פרטי, מקומי־תחילה (LLM מקומי) | ללא | ללא | API ציבורי בלבד | API ציבורי בלבד |
| **מהירות מצטברת** | \<200ms (מטמון חם) | \<100ms | \<150ms | \~1.5s | \~140ms |
| **אינטראקטיביות דינמית** | איי שרת (יעדי WASM) | ללא | ללא | איי שרת (JS) | איים (JS) |
| **מנוע חיפוש** | חיפוש WASM סמנטי מקומי | מחרוזת פשוטה | מחרוזת פשוטה | Pagefind (JS) | Pagefind (JS) |

---

## מיצוב ב־1.0

ב־1.0, המיצוב המיועד הוא מחולל אתרים סטטיים המהונדס כתשתית תוכנה מאובטחת כברירת מחדל: כתיבה הנתמכת בצנרות AI מקומיות־תחילה; הידור של 100,000 עמודים ומעלה דרך צנרת זורמת מקבילית; WCAG 2.2 AA ו־CSP ו־SRI מחמירים הנאכפים כשערי בנייה; ואיים דינמיים בארגז חול, הכול בתוך בינארי Rust יחיד ובטוח־זיכרון. כל פסוקית באמירה זו ממופה לפריט מסוים במפת הדרכים לעיל ולא לשאיפה שיווקית.

---

## שילוב רגולטורי ותאימות

במגזרים ארגוניים ופיננסיים בעלי סיכון גבוה, תוכנה נמדדת מבעד לעדשת התאימות והון הסיכון. מפת הדרכים הארכיטקטונית של `static-site-generator` מתואמת ישירות עם מנדטים רגולטוריים מרכזיים:

- **DORA סעיף 6 (ניהול סיכוני ICT):** החישוב וההזרקה בזמן קומפילציה של גיבובי SHA-384 SRI ומדיניות אבטחת תוכן מחמירה מספקים את הדרישה להגן על ערוצי הפרסום הדיגיטליים מפני הזרקת שרשרת אספקה, השחתת אתר ווקטורי סקריפטים חוצי־אתרים (XSS).  
- **DORA סעיף 7 (חוסן מערכות ICT):** במעבר לנכסים סטטיים בלתי־ניתנים לשינוי ומאומתים בזמן קומפילציה, מוסדות פיננסיים מחסלים פגיעויות של מסדי נתונים ושרתי ריצה, מפחיתים את מכפיל הסיכון התפעולי ומצמצמים את עתודות הון הסיכון הנדרשות תחת Basel III.  
- **חוק הנגישות האירופי (EAA) הנחיה (EU) 2019/882:** הזזת ביקורת הנגישות שמאלה אל תוך צנרת הקומפילציה כשער מהדר קשיח מבטיחה תאימות של 100% טרם הפריסה, ומחסלת את סיכון הנזק למותג וההתדיינות האזרחית תחת EAA ותחת ADA Title III.  
- **GDPR סעיף 25 (פרטיות בעיצוב):** הרצת צנרת התרגום והמטא־נתונים על חומרה מקומית ומבודדת־רשת מרחיקה טיוטות קנייניות, מדדים פיננסיים ונתונים אישיים מספקי LLM ציבוריים בענן של צד־שלישי, ותומכת בתאימות לעקרונות ריבונות הנתונים.

---

## שאלות נפוצות

**מה גרסה 0.0.41 אכן מספקת כיום, לעומת מה שה־README טוען?**
מודל האבטחה והנגישות ממשי ונאכף בקוד: `forbid(unsafe_code)` בכל סביבת העבודה, יצירת SHA-256/384 SRI, חילוץ CSP, שחרורים חתומים עם אישור Sigstore ו־SBOM מסוג CycloneDX, ושער WCAG 2.2 AA העוצר את הבנייה. שלוש תכונות מתועדות אינן מתפקדות בגרסה 0.0.41. ה־`MinifyPlugin` הוא מכווץ רווחים לבנים ולא מזער מודע־תחביר; ה־`DepGraph` שאמור להניע בנייה מצטברת עובר קומפילציה אך אינו מאוכלס לעולם בקוד הייצור; וקידוד AVIF הוא גדם שבו `avif_variants` מחזיר וקטור ריק.

**האם שער הנגישות הוא שער מהדר ממשי או לינטר שלאחר הבנייה?**
זהו שער בנייה. בדיקות WCAG 2.2 AA רצות בתוך צנרת הקומפילציה באמצעות מנתח axe-core בזמן בנייה המונע על ידי Playwright, ועמוד כושל עוצר את הקומפילציה עם שגיאות המציינות מספרי שורה מדויקים ולא פולט אזהרה בדיעבד. זו התכונה שחובת חוק הנגישות האירופי דורשת: פלט לא־תואם אינו יכול להגיע לפריסה.

**מדוע חשובה הקריאה החיצונית ל־`curl` בתוסף ה־LLM?**
צנרת ה־LLM המקומית (`src/plugins/llm.rs`) מפעילה את הבינארי `curl` של המארח כדי להגיע לנקודות קצה מקומיות. הדבר צומד את הבנייה לקובץ הרצה של המארח, נכשל במערכות שאין בהן `curl` ב־PATH, מכניס משטח הזרקת שֶׁל, ונשבר ב־CI מבודד־רשת. מעבר הקריאה ללקוח HTTP של Rust כגון `ureq` מסיר את התלות החיצונית ואת וקטור ההזרקה, ולכן זהו הפריט השני בתיקון 0.0.42.

**מהו הפריט היחיד החשוב ביותר בדרך אל 1.0?**
אכלוס ה־`DepGraph` וחיבור דגל ה־`--incremental`. בנייה מצטברת היא פער האמינות בין המנוע המתועד למנוע בפועל, וכל טענה במורד הזרם על בנייה בפחות משנייה ב־100,000 עמודים ומעלה תלויה בכך שגרף התלות יעקוב אחר קשתות תבנית־לעמוד ו־markdown־לעמוד, במקום להישאר תשתית לבדיקות בלבד.

## הפניות

- [Cloudflare, *lol-html: כותב־מחדש HTML זורם בהשהיית־פלט נמוכה*](https://github.com/cloudflare/lol-html "Cloudflare lol-html — כותב־מחדש HTML זורם") ⧉. [כותב־מחדש ה־HTML הזורם וללא ההעתקה המוצע להחליף את מניפולציית המחרוזות השבירה בשלב 0.1.0.]
- [W3C, *הנחיות הנגישות לתוכן ברשת (WCAG) 2.2*](https://www.w3.org/TR/WCAG22/ "W3C — המלצת WCAG 2.2") ⧉. [קריטריוני ההצלחה ברמה AA הנאכפים על ידי שער הנגישות בזמן קומפילציה.]
- [האיחוד האירופי, *תקנה (EU) 2022/2554 (DORA)*](https://eur-lex.europa.eu/eli/reg/2022/2554/oj "EUR-Lex — חוק החוסן התפעולי הדיגיטלי") ⧉. [סעיפי ניהול סיכוני ה־ICT והחוסן שאליהם ממופה תנוחת האבטחה.]
- [OpenSSF, *רמות שרשרת אספקה לפריטי תוכנה (SLSA) v1.0*](https://slsa.dev/spec/v1.0/ "SLSA — מפרט v1.0") ⧉. [מסגרת ייחוס הבנייה המיועדת לאישור בר־אימות ברמה 3 ב־1.0.]
- [Armin Ronacher, *מנוע התבניות MiniJinja*](https://github.com/mitsuhiko/minijinja "MiniJinja — מנוע Jinja2 מזערי ל־Rust") ⧉. [המנוע דל־התלויות שהחליף את Tera וגזם את עץ המעבר.]
- [CycloneDX, *מפרט רשימת רכיבי תוכנה v1.5*](https://cyclonedx.org/docs/1.5/ "CycloneDX — מפרט SBOM v1.5") ⧉. [פורמט ה־SBOM הנפלט בכל בנייה לביקורת שרשרת אספקה.]
- [האיחוד האירופי, *הנחיה (EU) 2019/882 (חוק הנגישות האירופי)*](https://eur-lex.europa.eu/eli/dir/2019/882/oj "EUR-Lex — חוק הנגישות האירופי") ⧉. [חובת הנגישות ששער ה־WCAG בזמן בנייה נועד לספק.]

*נבדק לאחרונה ביולי 2026. הניתוח המקורי מבוסס על בדיקת בסיס הקוד של `static-site-generator` בגרסה 0.0.41; המקורות מצוטטים, לא משוכפלים. מספרי גרסה וסטטוס תכונות משתנים במהירות, יש לאמת מול המאגר לפני פרסום חוזר. מורשה תחת CC-BY-4.0.*

